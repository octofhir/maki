# SUSHI Configuration for comparison testing
id: my.example.test-build-ig
canonical: http://example.org/fhir/examples/test-build-ig
name: TestBuildIG
title: "Test Build IG"
version: 0.1.0
status: draft
publisher: Example Publisher
fhirVersion: 4.0.1

# Dependencies
dependencies:
  hl7.fhir.r4.core: 4.0.1

# Use FSHOnly mode to generate only resources (no IG Publisher)
FSHOnly: true

# Parameters
parameters:
  show-inherited-invariants: false

# Resource configuration
copyrightYear: 2025+
releaseLabel: ci-build
